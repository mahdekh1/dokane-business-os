'use client';

import { ComingSoon } from '../../../../src/components/coming-soon';

export default function AdminAuditPage() {
  return <ComingSoon title="Audit log" blurb="Security-sensitive events across the platform (approvals, suspensions, role changes)." />;
}
