/**
 * Shared configuration + environment parsing.
 *
 * Storage drivers (local/S3), typed env parsing and shared constants are added
 * in later phases (see docs/IMPLEMENTATION_PLAN.md). Reserved during scaffolding.
 */
export const CONFIG_VERSION = '0.0.0';
