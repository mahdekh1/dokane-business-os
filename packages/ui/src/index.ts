/**
 * Shared UI package: components + design tokens/theming.
 *
 * Components (Button, DataTable, MoneyDisplay, theme provider, …) are added in
 * Phase 1+ (see docs/IMPLEMENTATION_PLAN.md). Reserved during scaffolding.
 */
export const UI_VERSION = '0.0.0';
