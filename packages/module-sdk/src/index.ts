/**
 * Module SDK.
 *
 * The full `ModuleManifest` type and `registerModule()` helper are implemented
 * in Phase 1 (Task 1.5 in docs/IMPLEMENTATION_PLAN.md). This package is
 * reserved now so app and package imports resolve during scaffolding.
 */
export const MODULE_SDK_VERSION = '0.0.0';
